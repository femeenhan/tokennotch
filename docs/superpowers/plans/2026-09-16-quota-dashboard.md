# 계정 한도 중심 대시보드

사용자 승인: 17:05 ‘다음 구현을 시작해’. 목적은 계정 잔여량과 리셋까지 남은 시간으로 Astra/Sol 사용을 판단하는 것이다.

1. Codex app-server 계정 한도를 읽기 전용으로 조회한다. 모델·작업 실행이나 인증 변경은 하지 않는다. duration으로 5시간300/일간1440/주간10080분을 식별하고, primary/secondary 위치로 추정하지 않는다. 별도 bucket은 구분한다. 미제공은 확인불가다.
2. 계정 한도는 앱 시작·패널 열기·수동 새로고침에 조회하며 평소60초 주기로 갱신한다. 조회 timeout·오류·마지막 성공시각·오래된 상태를 표시한다. 재실행 시 관측 이력을 복구하되 새 조회 전 ‘이전 기록’으로 표시한다.
3. 로컬에 최대8일의 한도 관측만 보관해 오늘과 최근60분의 감소량을 계산한다. 같은bucket/같은주간reset만 비교하고 감소·리셋 경계는 추세를 무효화한다. 최초 측정부터 계산하며 자정0을 가정하지 않는다. 충분한 표본이 있을 때만 조건부 소진 위험을 안내한다.
4. 메인화면은 usage, 도구활용, 상세기록 탭. 주간잔여를 크게 표시하고 리셋카운트다운/정확시각/단기한도/관측상태를 붙인다. 활동격자는 제거하고 JSON내보내기는 설정으로 이동한다. 캐릭터클릭은 작은usage패널, 기존관리메뉴는 패널/메뉴막대에서 접근한다.
5. MCP는 현재훅의 정확한범주/완료/실패만 표시한다. Skills 사용근거를 확보하지 못하면 미관측으로 표시하고 총횟수를 지어내지 않는다. 서버별/스킬별 정밀관측은 이번확인결과에 따라 보류한다.
6. fixture프로토콜/timeout/리셋/저장복원/집계 테스트, 실제계정읽기, Xcode빌드, 기존통합테스트, 실제UI검증을 실시한다. 커밋·푸시하지 않는다.

## 실행 결과

2026-09-16 구현과 독립리뷰 완료. Swift Testing은111개/16suite 실패0으로 종료했고 기본제외된 실제계정smoke는 별도명시실행으로 성공했다. 브리지4/앱훅3/대시보드1/계정한도1 통합테스트 성공. Xcode Debug Swift6 build exit0. 실제계정조회, 일반주간과Spark별도한도 구분, 밝은/어두운화면, 자동조회중지·재개(원래설정복원), 작은패널→대시보드전환 확인. 오류·과거관측·동일계정·기간필터범위 리뷰 지적을 수정했다. 정수계단 사용률과 실제응답reset시각1초흔들림 회귀도 추가해 최대5초허용을 최신reset에 고정했으며 큰reset변경은 무효화한다. 일반실행으로 재시작해 대시보드를 열어두었다. 사용법: docs/dashboard.md. 커밋·푸시없음.
